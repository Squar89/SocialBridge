version: '3.8'

services:
  frontend:
    image: frontend
    build:
      context: ../frontend
      args:
        BACKEND_URL: http://localhost:8000
    container_name: frontend
    ports:
      - "3000:3000"
    networks:
      - app_network


networks:
  app_network:
    driver: bridge
